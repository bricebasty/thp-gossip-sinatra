<html>
  <head>
    <title>Potin numéro <%= id %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
  </head>
  <body class="m-3">
    <h1 class="my-3">Voici la page du potin n°<%= id %></h1>
    <p>Son auteur : <strong><%= gossips[id - 1].author %></strong></p>
    <p>Le contenu du potin : <strong><%= gossips[id - 1].content %></strong></p>
    <a class="btn btn-warning" href="/gossips/<%= id %>/edit">Edite ce potin</a><br>
    <% if comments.any? { |comment| comment.gossip == id } %>
      <h2 class="mt-3">Commentaires</h2>
    <% end %>
    <% comments.each do |comment| %>
      <% if comment.gossip == id.to_i %>
        <p>
          <strong><%= comment.author %></strong><br>
          <%= comment.content %>
        </p>
      <% end %>
    <% end %>
    <h2 class="mt-3">Ecris un commentaire</h2>
    <form action='/gossips/<%= id %>/comment/new' method='POST'>
      <input class="m-2" value="Ton nom" type='text' name='comment_author' autocomplete="name"><br>
      <input class="m-2" value="Ton commentaire" type='text' name='comment_content' autocomplete="off"><br>
    <input class="btn btn-success" type='submit' value="Envoyer">
    </form>
    <a class="btn btn-primary" href="../../">Lien pour revenir à la page des potins</a>
  </body>
</html>
